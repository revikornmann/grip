# Muka UI Guidelines

> Generated by `muka-ui init` on 2026-02-04

This document describes how to use Muka UI components in this project.

---

## Installation

Muka UI should already be installed. If not:

```bash
npm install muka-ui
```

For local development with the muka-ui source:

```bash
npx muka-ui link /path/to/muka
```

---

## Importing Components

```tsx
// Import individual components
import { Button, Card, Input, Badge, Select } from '@revikornmann/muka-ui';

// Import types for TypeScript
import type { ButtonProps, InputProps } from '@revikornmann/muka-ui';
```

---

## Importing Styles

Import styles once in your app's root layout or entry point:

```tsx
// Default theme (muka-light)
import '@revikornmann/muka-ui/styles';

// Or choose a specific theme:
import '@revikornmann/muka-ui/styles/muka-light.css';
import '@revikornmann/muka-ui/styles/muka-dark.css';
import '@revikornmann/muka-ui/styles/wireframe-light.css';
import '@revikornmann/muka-ui/styles/wireframe-dark.css';
```

### Theme Switching

To support dynamic theme switching, import all themes and toggle via a CSS class or attribute:

```tsx
// In your root layout
import '@revikornmann/muka-ui/styles/muka-light.css';
import '@revikornmann/muka-ui/styles/muka-dark.css';

// Apply theme via data attribute or class
<html data-theme="muka-dark">
```

---

## Available Components

| Component | Description | Key Props |
|-----------|-------------|-----------|
| `Button` | Primary, secondary, and ghost button variants | variant, size, disabled... |
| `Input` | Text input with label, helper text, and error states | label, placeholder, helperText... |
| `Card` | Content container with consistent padding and styling | children, className |
| `Badge` | Status indicators and labels | variant, size, children |
| `Label` | Form field labels | htmlFor, required, children |
| `Icon` | Icon wrapper for consistent sizing | name, size, className |
| `Divider` | Visual separator between content sections | orientation, className |
| `Select` | Dropdown select with options | options, value, onChange... |
| `Tabs` | Tabbed interface container | defaultValue, value, onChange... |
| `ListItem` | List item with consistent styling | children, onClick, selected... |

### Planned Components

| Component | Roadmap |
|-----------|---------|
| Modal | #15 |
| Alert | #24 |
| Toast | #24 |
| Table | #12 |
| Spinner | #27 |
| Skeleton | #29 |
| Tooltip | #18 |
| Progress | #28 |
| EmptyState | #30 |
| DatePicker | #21 |

For full documentation and interactive examples, visit: https://storybook.mukaui.com

---

## Using Design Tokens

Muka UI provides CSS custom properties (tokens) for consistent styling:

### Spacing

```css
var(--spacing-1)   /* 0.25rem */
var(--spacing-2)   /* 0.5rem */
var(--spacing-3)   /* 0.75rem */
var(--spacing-4)   /* 1rem */
var(--spacing-5)   /* 1.25rem */
var(--spacing-6)   /* 1.5rem */
var(--spacing-8)   /* 2rem */
var(--spacing-10)  /* 2.5rem */
var(--spacing-12)  /* 3rem */
```

### Colors (Semantic)

```css
/* Text */
var(--color-text-default-default)
var(--color-text-subtle-default)
var(--color-text-inverse-default)

/* Backgrounds */
var(--color-surface-level1)
var(--color-surface-level2)
var(--color-surface-level3)

/* Borders */
var(--color-border-default)
var(--color-border-contrast)
var(--color-border-focus)

/* Actions */
var(--color-action-default)
var(--color-action-hover)
var(--color-action-pressed)
```

### Typography

```css
var(--alias-font-brand-family)
var(--alias-font-brand-weight-semibold)
var(--text-label-sm-fontSize)
var(--text-label-md-fontSize)
var(--text-label-lg-fontSize)
```

---

## Requesting New Components

If you need a component that doesn't exist in Muka UI:

1. **Check Storybook** to confirm it's not available: https://storybook.mukaui.com

2. **Don't create a local workaround** — this defeats the purpose of using a design system

3. **Request the component** by:
   - Opening an issue in the muka-ui repository
   - Or building it yourself in muka-ui and submitting a PR

4. **Wait for availability** — once the component is in Storybook, you can use it

---

## Common Patterns

### Form with Validation

```tsx
import { Input, Button, Card } from '@revikornmann/muka-ui';

function LoginForm() {
  const [error, setError] = useState('');

  return (
    <Card>
      <Input
        label="Email"
        type="email"
        error={!!error}
        errorMessage={error}
      />
      <Input
        label="Password"
        type="password"
      />
      <Button variant="primary" fullWidth>
        Log in
      </Button>
    </Card>
  );
}
```

### Button Variants

```tsx
import { Button } from '@revikornmann/muka-ui';

// Primary action
<Button variant="primary">Save</Button>

// Secondary action
<Button variant="secondary">Cancel</Button>

// Subtle action
<Button variant="ghost">Learn more</Button>

// Sizes
<Button size="sm">Small</Button>
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
```

### Tabbed Interface

```tsx
import { Tabs, TabList, Tab, TabPanel } from '@revikornmann/muka-ui';

<Tabs defaultValue="tab1">
  <TabList>
    <Tab value="tab1">First</Tab>
    <Tab value="tab2">Second</Tab>
  </TabList>
  <TabPanel value="tab1">First tab content</TabPanel>
  <TabPanel value="tab2">Second tab content</TabPanel>
</Tabs>
```

---

## Resources

- **Storybook:** https://storybook.mukaui.com
- **GitHub:** https://github.com/uxdelta/muka
- **Package:** muka-ui v0.1.0
